# Supabase: public browser configuration. When omitted, Nitro serves the built-in
# development portfolio (five fixture companies) instead of failing.
NUXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NUXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Optional server-only configuration; never use a NUXT_PUBLIC_ prefix.
NUXT_SUPABASE_SECRET_KEY=
NUXT_SUPABASE_JWKS_URL=https://your-project.supabase.co/auth/v1/.well-known/jwks.json

# X-Ray assistant (server-only). All three values are required.
AGENT_BASE_URL=https://api.helmcode.com/v1
AGENT_MODEL=deepseek-v4-flash
AGENT_API_KEY=
